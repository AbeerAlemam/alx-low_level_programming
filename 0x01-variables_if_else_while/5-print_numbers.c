
#include <stdio.h>

/**
 * main - entry point
 *
 * Description: prints all alphabet do not prints q,e.
 *
 * Return: Always 0 (success)
*/

int main(void)
{
	int digit = 0;

	while (digit <= 9)
	{
		printf("%i", digit);
		digit++
	}
	printf("\n");

	return (0);
}
