#include <stdio.h>

/**
 * main - a function-like macro ABS(x) that computes
 * the absolute value of a number x
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
